# The operator runs alongside everything it manages, in the "shops" namespace — the same
# namespace shophub-app's KubernetesOptions.Namespace (default "shops") creates Shop/Wallet/
# DiscordChannel custom resources in.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: shophub-shop-operator
  namespace: shops
